#### method 1)  sort the whole vector if there are any adcent equal elements then return true else after checking all elements return false

#### method2) Transverse the whole vector one by one keep putiing the elements in a map and any elemet already exist in the map return true </br>else after transversing all elements return false.
